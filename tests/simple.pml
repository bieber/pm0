const m = 7, n = 85;  
int  i,x,y,z,q,r;  
procedure mult; 
   int a, b;  
  snga'i 
  const m = 7, n = 85;  
int  i,x,y,z,q,r;  
procedure mult; 
   int a, b;  
  snga'i 
     a := x;b:=y; z := 0;
    tengkrr b > 0 si
     snga'i
        txo odd x tsakrr z := z+a;
           a := 2*a;
           b := b/2;
     fpe'   
  fpe';

snga'i
  x := m;
  y := n;
  syaw mult;
fpe'.
     a := x;  b:= y; z := 0;   
     tengkrr b > 0 si    
     snga'i 
        txo odd x tsakrr z := z+a;       
           a := 2*a; 
           b := b/2;     
     fpe'   
  fpe';

snga'i
  x := m;
  y := n;
  syaw mult;
fpe'.
